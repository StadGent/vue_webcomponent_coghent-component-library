export declare type ImageSource = {
    imageUrl: string;
    infoJson: string;
    fallBackUrl: string;
    noImageUrl: string;
    mediaType: object;
};
export declare type ModalState = "show" | "hide" | "loading" | "small";
